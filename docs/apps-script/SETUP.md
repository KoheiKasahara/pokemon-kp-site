# セットアップ確認

- Apps Scriptへ `publish-site.gs` をコピーした
- スクリプトプロパティ `GITHUB_TOKEN` を設定した
- GitHubトークンの対象リポジトリを `pokemon-kp-site` のみに限定した
- スプレッドシートのボタンへ `publishSite` を割り当てた
- 初回の権限承認を完了した
- GitHub Actionsで `Deploy GitHub Pages` が開始されることを確認した
